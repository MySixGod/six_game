[
  {six_game, []},
  {mysql_poolboy, [
      {pool1, {[{size, 10}, {max_overflow, 20}],
        [{user, "root"}, {password, "cwtborn123"}, {database, "hello"},
          {prepare, [{hello, "SELECT * FROM hello_table"}]}]}}
    ]}
].
